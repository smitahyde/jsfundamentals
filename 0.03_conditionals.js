// Conditionals

/*
Chaninging conditional operators
--------------------------------
&&

*/

var name= "smita";

if ("Smita == name") {
console.log("Hey your name is"+ name);
} else {
	console.log("What is your name?");
};

// Create an if statement that checks if the users name does not have a capital letter,
// Rebuild their name with the capitalized first letter

var fname = "smita";

if (fname [0]!=fname[0].toUpperCase());{
	if (fname [0]!=fname.toUpperCasse () + fname [2] + fname [3];
		console.log ("Horrible puncation, you need to uppercase the first letter like this:" 
} else {
	console.log ("Great puncation!");
}

var age =16
if (age >= 18) {
	console.log ("Yay you can vote!");
} else if (age >=21) {
	console.log ("Yay you can drink!");
} else if (age >= 25) {
	console.log ("Yay you get lower rates on your car rental");
}

